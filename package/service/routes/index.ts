import Root from '$/index.vue'

const serviceRootRoute = {
  path: 'service-root',
  name: 'service-root',
  component: Root,
  meta: {
    title: '业务模块',
  },
  children: [],
}

export default serviceRootRoute
